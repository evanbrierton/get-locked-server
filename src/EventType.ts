enum EventType {
  HOST = "host",
  JOIN = "join",
  CODE = "code",
  ERROR = "error",
  CARDS = "cards",
  SETTINGS = "settings",
  PROGRESS = "progress",
  UPDATE = "update",
  START = "start",
  CONNECT = "connect"
}

export default EventType;